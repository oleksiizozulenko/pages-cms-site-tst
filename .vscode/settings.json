{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "inmech",
            "database": "${workspaceFolder:input}/db.sqlite"
        }
    ]
}